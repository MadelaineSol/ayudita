/**
 * Configuración del frontend.
 * En producción, API_URL apunta al dominio del backend.
 */
export const API_URL = (window.AYUDITA_API_URL || 'http://localhost:8080') + '/api/v1';
export const APP_NAME = 'Ayudita';
